from django.apps import AppConfig


class DocusafeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.docusafe"
    label = "docusafe"
    verbose_name = "Docusafe"
